fastapi>=0.110.0
uvicorn[standard]>=0.29.0
openai>=1.12.0
pydantic>=2.6.0
edge-tts>=6.1.9
pygame>=2.5.0

# STT (speech-to-text) — optional but recommended.
# Comment out individual lines if you don't want a given provider.
# faster-whisper drags ctranslate2 (~30 MB) and downloads a model on first use
# (base ~150 MB, small ~500 MB). sounddevice is required for mic capture.
faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.24.0
